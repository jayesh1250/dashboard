apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "react-app.fullname" . }}
  namespace: {{ .Values.namespace.name }}
spec:
  rules:
    - host: react.app.com # Replace with your domain
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: react-app-service
                port:
                  number: 80
